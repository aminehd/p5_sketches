Art Sketeches and Gifs generated by p5.js library

You can find more functionalities in modules under utils.

You can use this configuration to generate p5 sketches locally:

    - Clone the repo locally.

    - Install npm and install http-server globally. 

    - Make a copy of template.js and rewrite p5._drawme function.

    - Add your new js file to index.html and run http-server to run that 

Example of Artwork:

    look at escher grid loop.gif and round harmony.gif